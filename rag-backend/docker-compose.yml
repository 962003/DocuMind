services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rag-backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - backend_uploads:/app/uploads

volumes:
  backend_uploads:
